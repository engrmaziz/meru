# Meru Workshop Web (Phase 9 Stamp)

Staff MVP per DEC-014 — Next.js on the same Nest API.

```bash
# terminal 1
cd backend && npm run start:dev

# terminal 2
cd workshop-web && npm install && npm run dev
# http://localhost:3001
```

Set token + workshop id from a registered Meru user (`Bearer meru_…`) and `X-Workshop-Id: ws-audi-lhr`.
